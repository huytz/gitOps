apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    # matrix 'parent' generator
   - matrix:
      generators:
      # git generator, 'child' #1
      - git:
          repoURL: https://github.com/huytz/gitops.git
          revision: HEAD
          directories:
          - path: apps/*/*/*
      # cluster generator, 'child' #2
      - clusters:
          selector:
            matchLabels:
              argocd.argoproj.io/secret-type: cluster
              kubernetes.io/environment: prod
  
  template:
    metadata:
      # annotations:
      #   argocd-image-updater.argoproj.io/image-list: ghcr.io/huytz/{{.path.basename}}
      #   argocd-image-updater.argoproj.io/write-back-method: git
      #   argocd-image-updater.argoproj.io/update-strategy: latest
      #   argocd-image-updater.argoproj.io/allow-tags: regexp:^v.*$
      #   argocd-image-updater.argoproj.io/update-interval: 5m

      name: '{{.name}}-{{index .path.segments 2}}-{{.path.basename}}'
      labels:
        appset: apps
        environment: 'prod'
        cluster: '{{.name}}'
    spec:
      project: apps      
      sources:
      - repoURL: https://helmcharts.gruntwork.io
        chart: 'k8s-service'
        targetRevision: "0.2.29"
        helm:
          releaseName: '{{.path.basename}}'
          valueFiles:
          - $values/{{.path.path}}/values.yaml 
      - repoURL: https://github.com/huytz/gitops.git
        targetRevision: HEAD
        ref: values

      destination:
        server: '{{.server}}'
        namespace: '{{index .path.segments 2}}'
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        automated:
          prune: true
          selfHeal: true
